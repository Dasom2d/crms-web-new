package com.kakaopay.crms.enums;

public enum CrmsCallType {
    SELECT_PAGE("페이지 검색"),
    SELECT_PAGE_ROW("페이지 검색"),
    SELECT_LIST("페이지 검색"),
    SELECT_INQUIRY_LIST("페이지 검색"),
    SELECT_LIST_GUBUN("페이지 검색");

    CrmsCallType(String callType) {
    }
}
